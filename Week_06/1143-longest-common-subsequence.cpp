class Solution {
public:
    int longestCommonSubsequence(string text1, string text2) {
        string sstr,lstr,rstr;

        if (tex1.length() > text2.length()) 
        {
            sstr = text2;
        } else {
            lstr = text1;
        }


        for (int i=0;i<sstr.length();i++) {
            char sc = sstr[i];

            for (int j=0;j<lstr.length();j++) {
                char lc = lstr[j];
                if (c == lc) {
                    
                }
            }
        }
    }
};